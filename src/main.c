#include <stdlib.h>
#include <stdio.h>

int
main (int argc, char *argv[])
{
    printf (
        "A kitten just died in atrocious pain.\n"
        "Please do not use sudo anymore.\n"
    );

    return EXIT_FAILURE;
}
